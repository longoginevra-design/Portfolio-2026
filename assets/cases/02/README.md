# Case 02 — Designing to last — asset shot list

Drop files into this folder using the filenames below. `case-02.html`'s `.ph`
placeholders reference these slots directly by description — once a file
lands, replace the corresponding `.ph` block with the real `<img>`/`<video>`
tag (keep `width`/`height` or `aspect-ratio` set to avoid layout shift).

| Filename | Slot | Ratio | Priority |
|---|---|---|---|
| `hero.jpg` | Multi-product HMI context | 21:9 | 1 |
| `variables-figma.jpg` | Variables structure in Figma, anonymised | 4:3 | 2 |
| `coworking.jpg` | Co-working session artefact — weakest slot, fine to skip and leave the "Trust" block type-only | 4:3 | 3 |

The pattern-documentation video slot was removed (brief 04, item 5) — section 04 on this page runs on stats and a closing line only, no media.
